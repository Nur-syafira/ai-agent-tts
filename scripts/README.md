# Scripts - Testing & Utilities

Утилиты и тесты для Sales Agent MVP.

## 🔍 Комплексное тестирование системы

### `test_system.py` - Полное пошаговое тестирование

Комплексный скрипт для проверки всех компонентов системы с детальными отчетами.

**Запуск:**

```bash
# Полное тестирование с остановкой при ошибке
uv run python scripts/test_system.py

# Продолжать выполнение при ошибках
uv run python scripts/test_system.py --continue-on-error

# Тестирование только определенных шагов
uv run python scripts/test_system.py --steps environment,local_models,asr_model
```

**Что проверяет:**

1. **environment** - Python, CUDA, GPU, установленные пакеты
2. **local_models** - Проверка локальных моделей F5-TTS и Qwen3 на диске
3. **asr_model** - Загрузка faster-whisper с измерением VRAM и латентности
4. **tts_model** - Загрузка F5-TTS с измерением VRAM и латентности
5. **llm_model** - Проверка локальной модели Qwen3 и конфигурации
6. **vram_all_models** - Одновременная загрузка всех моделей с мониторингом VRAM
7. **redis** - Проверка подключения и работы Redis
8. **vllm_server** - Проверка vLLM сервера с измерением TTFT и latency
9. **asr_gateway** - Проверка ASR Gateway health и readiness
10. **tts_gateway** - Проверка TTS Gateway с тестовым синтезом
11. **policy_engine** - Проверка Policy Engine с тестовым диалогом
12. **freeswitch_bridge** - Проверка FreeSWITCH Bridge через API и WebSocket
13. **e2e_dialog** - Полный E2E тест диалога с детальными метриками

**Отчеты:**

После выполнения создаются два файла в `test_reports/`:
- `test_report_YYYYMMDD_HHMMSS.md` - человекочитаемый отчет
- `test_report_YYYYMMDD_HHMMSS.json` - машинночитаемый для автоматизации

**Пример вывода:**

```
======================================================================
🔍 environment
======================================================================
   Python: 3.12.0 ✅
   CUDA: 12.1 ✅
   GPU устройств: 1
      GPU 0: NVIDIA RTX 5090
      VRAM: 32.00 GB
✅ environment - успешно (2.34s)
   VRAM: 1024 MB → 1024 MB (Δ +0 MB)
```

---

### `test_vram_stress.py` - Стресс-тест VRAM

Проверяет стабильность работы всех моделей при одновременной загрузке.

**Запуск:**

```bash
# Стресс-тест на 5 минут (по умолчанию)
uv run python scripts/test_vram_stress.py

# Стресс-тест на 10 минут
uv run python scripts/test_vram_stress.py --duration 10
```

**Что делает:**

- Загружает все модели одновременно (ASR, TTS, vLLM)
- Мониторит использование VRAM каждые 10 секунд
- Выявляет возможные утечки памяти
- Генерирует отчет о стабильности

**Важно:** vLLM сервер должен быть запущен отдельно перед запуском стресс-теста.

---

## 🔍 Проверка моделей

### `check_models.py` - Проверка доступности моделей

Проверяет все ли модели скачаны и готовы к работе:

```bash
uv run python scripts/check_models.py
```

Проверяет:
- ✅ CUDA
- ✅ faster-whisper large-v3-turbo
- ✅ Silero VAD
- ✅ F5-TTS (локальная модель)
- ✅ Qwen3-16B-A3B-abliterated-AWQ (локальная модель или кэш)

**Вывод:**
```
✅ All critical components ready!
```

---

## 🧪 Тестирование диалога

### `test_dialog_simulator.py` - Автоматический симулятор диалога

Симулирует полный диалог между AI-агентом и пациентом.

**Запуск:**

```bash
# 1. Запусти сервисы
docker-compose up -d redis
vllm serve models/Qwen3-16B-A3B-abliterated-AWQ --host 0.0.0.0 --port 8000 --quantization awq --enable-chunked-prefill --enable-prefix-caching  # в отдельном терминале
uv run python src/policy_engine/main.py    # в отдельном терминале
uv run python src/tts_gateway/main.py      # опционально

# 2. Запусти симулятор
uv run python scripts/simulate_dialog.py
```

**Что делает:**
- Автоматически проходит через 20+ turns диалога
- Симулирует ответы пациента
- Показывает ответы агента
- Отображает заполнение слотов
- Проверяет завершение диалога

**Пример вывода:**
```
================================================================================
Turn 1
================================================================================
👤 Пациент: Здравствуйте, хочу записаться на МРТ головного мозга
🤖 Агент: Добрый день! Меня зовут администратор медицинского центра МРТ 1.5Т. Как к вам обращаться?
📊 State: ask_client_name
📝 Заполнено слотов: 1
   - study_type: МРТ головного мозга
```

---

### `test_voice_chat.py` - Тест через микрофон (интерактивный)

Позволяет **реально поговорить с AI-агентом** через микрофон.

**Запуск:**

```bash
# 1. Запусти ВСЕ сервисы (включая ASR и TTS)
docker-compose up -d redis jaeger
vllm serve Qwen/Qwen2.5-14B-Instruct-AWQ ...
./venv/bin/python src/asr_gateway/main.py
./venv/bin/python src/tts_gateway/main.py
./venv/bin/python src/policy_engine/main.py

# 2. Запусти тест
./venv/bin/python scripts/test_voice_chat.py
```

**Что делает:**
- Проверяет все сервисы
- Записывает твою речь с микрофона (5 сек)
- Распознаёт через ASR (пока mock - вводишь текст вручную)
- Отправляет в Policy Engine
- Получает ответ агента
- Синтезирует речь через F5-TTS
- Воспроизводит через динамики

**Примечание:** ASR WebSocket пока не реализован в тесте, поэтому вместо записи с микрофона нужно вводить текст вручную. Но TTS работает полностью - агент ответит голосом!

---

## 🛠️ Утилиты

### `setup_env.sh` - Системные оптимизации

Настраивает GPU persistence и CPU performance mode:

```bash
sudo ./scripts/setup_env.sh
```

### `download_models.py` - Скачивание моделей

Скачивает все необходимые модели:

```bash
uv run python scripts/download_models.py
```

Скачивает:
- faster-whisper large-v3-turbo
- Qwen3-16B-A3B-abliterated-AWQ (в локальную директорию)
- F5-TTS модель должна быть размещена вручную в `models/F5-tts/`
- Silero VAD

### `health_check.py` - Проверка здоровья системы

Проверяет все сервисы и систему:

```bash
uv run python scripts/health_check.py
```

Показывает:
- Состояние окружения (Python, CUDA, credentials)
- GPU статус (VRAM, температура, загрузка)
- System stats (CPU, RAM)
- Redis статус
- Статус всех сервисов (ASR, LLM, TTS, Policy)

---

## 📋 Быстрый старт тестирования

### Вариант 1: Комплексное тестирование (рекомендуется)

```bash
# 1. Запусти Redis
docker-compose up -d redis

# 2. Запусти vLLM сервер (в отдельном терминале)
vllm serve models/Qwen3-16B-A3B-abliterated-AWQ \
  --host 0.0.0.0 --port 8000 \
  --max-model-len 2048 --gpu-memory-utilization 0.75 \
  --quantization awq --enable-chunked-prefill --enable-prefix-caching

# 3. Запусти все сервисы (в отдельных терминалах)
uv run python src/asr_gateway/main.py &
uv run python src/tts_gateway/main.py &
uv run python src/policy_engine/main.py &
uv run python src/freeswitch_bridge/main.py &

# 4. Запусти комплексное тестирование
uv run python scripts/test_system.py
```

### Вариант 2: Симуляция диалога (быстрый тест)

```bash
# Минимум сервисов
docker-compose up -d redis
vllm serve models/Qwen3-16B-A3B-abliterated-AWQ --host 0.0.0.0 --port 8000 --quantization awq &
uv run python src/policy_engine/main.py &

# Запуск симулятора
uv run python scripts/simulate_dialog.py
```

### Вариант 3: Стресс-тест VRAM

```bash
# Запусти все модели (ASR, TTS, vLLM)
# Затем запусти стресс-тест
uv run python scripts/test_vram_stress.py --duration 10
```

---

## 🎯 Что тестировать

1. **Комплексное тестирование** - `test_system.py` проверяет все компоненты
2. **Проверка моделей** - `check_models.py` убедись что всё скачано
3. **Симулятор** - `simulate_dialog.py` проверь что диалог проходит все этапы
4. **Health check** - `health_check.py` убедись что все сервисы healthy
5. **Стресс-тест VRAM** - `test_vram_stress.py` проверь стабильность памяти
6. **Google Sheets** - проверь что данные записываются в Лист4

---

## 📊 Ожидаемые метрики

| Метрика | Цель | Как проверить |
|---------|------|---------------|
| ASR latency | 80-150 мс | `scripts/health_check.py` |
| LLM latency | 40-150 мс | Jaeger UI (localhost:16686) |
| TTS latency | 50-120 мс | Измерить в `test_voice_chat.py` |
| E2E dialog turn | 250-600 мс | Симулятор |
| Dialog completion | 100% | Симулятор завершается успешно |
| Slots filled | 10+ | Симулятор показывает слоты |

---

## 🐛 Troubleshooting

### Симулятор: "Policy Engine не запущен"

```bash
# Проверь что Policy Engine запущен
curl http://localhost:8003/health

# Если нет - запусти
./venv/bin/python src/policy_engine/main.py
```

### Voice chat: "TTS Gateway error"

```bash
# Проверь TTS Gateway
curl http://localhost:8002/health

# Если нет - запусти
./venv/bin/python src/tts_gateway/main.py
```

### Модели не найдены

```bash
# Скачай модели
./venv/bin/python scripts/download_models.py

# Проверь
./venv/bin/python scripts/check_models.py
```

---

**Готов к тестированию!** 🚀

